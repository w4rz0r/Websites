/* let increaseButton = `${dec}`;
let decreaseButton = `${inc}`;

for(let i = 0; i < increaseButton.length; i++){
    let button = increaseButton[i];
    
}
    */



const model = {
    state: {
        page: 'home'
    },
    inputs: {
        message: ''
    },
    messages: []
};